apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  APP_ENV: "production"
  APP_DEBUG: "false"
  config.yaml: |
    server:
      port: 8080
      host: "0.0.0.0"

